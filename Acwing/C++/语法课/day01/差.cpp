#include <iostream>
#include <cstdio>

using namespace std;

int main()
{
    int a,b,c,d;
    int DIFERENCA;
    cin >> a;
    cin >> b;
    cin >> c;
    cin >> d;
    DIFERENCA = a*b-c*d;
    printf("DIFERENCA = %d",DIFERENCA);
    return 0;
}